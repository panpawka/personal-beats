import type { VerificationEmailProps } from './VerificationEmail';

export const mockVerificationProps: VerificationEmailProps = {
  verificationLink: 'https://feednode.app/verify-email?token=abc123xyz',
};
